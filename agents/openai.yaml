interface:
  display_name: "Analytical"
  short_description: "Derive and verify mechanistic models"
  default_prompt: "Use $analytical to develop or audit a mechanistic model from balances, assumptions, primary sources, and discriminating benchmarks."
